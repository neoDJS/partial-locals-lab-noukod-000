
<% @students.each do |s| %>
  <%= render partial: "form", locals: {student: s} %>
<% end %>

<%= form_tag students_path, method: :get do %>
  <p>
    <%= text_field_tag :student, @ask %>
    <%= submit_tag "Search" %>
  </p>
<% end %>
