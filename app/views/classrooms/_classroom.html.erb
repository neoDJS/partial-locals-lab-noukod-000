Classroom Info
<p>
<%= classroom.course_name %> 
<%= classroom.semester %>
</p>

We now want to call out the oldest student in the class:
<%= render partials: 'students/student', locals: {student: classroom.oldest_student} %>


Student list:
<% classroom.students.each do |s| %>
  <%= render partials: 'students/student', locals: {student: s} %>
<% end %>
